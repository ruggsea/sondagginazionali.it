[project]
name = "sondagginazionali"
version = "0.1.0"
description = "Italian polls visualization"
requires-python = ">=3.10"
dependencies = [
    "python-fasthtml",
    "pandas",
    "numpy",
    "redis",
    "python-dateutil"
]


